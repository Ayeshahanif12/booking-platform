version: '3.8'
services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=development
      - MONGODB_URI
      - JWT_SECRET
    volumes:
      - ./:/app
    command: npm run dev
